import { Loading as GeistLoading } from '@geist-ui/core';

const Loading = () => {
    return (
        <GeistLoading style={{ width: '100%', height: '80%', zoom: '200%' }} />
    );
};

export default Loading;
